apiVersion: V1
kind: Pod
metadata:
  name: labels
  namespace: roboshop
  labels:
    project: roboshop
    componennt: frontend
    environment: dev
    purpose: poc
spec:
  containers:
  - name: nginx
    image: nginx
