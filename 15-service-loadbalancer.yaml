apiVersion: v1
kind: Service
metadata:
  name: nginx-nodeport
 #namespace: roboshop
spec:
  type: NodePort
  selector:
    project: roboshop
    component: frontend
    environment: dev
  ports:
   - protocol: TCP
     port: 80 #port on which the service is exposed
     targetPort: 80 