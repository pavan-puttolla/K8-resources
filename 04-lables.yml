apiVersion: v1
kind: Pod
metadata:
  name: labels
  namespace: roboshop
  labels:
      project: roboshop
      component: frontend
      environment: dev
      purpose: poc
spec:
  containers:
  - name: nginx
    image: nginx
